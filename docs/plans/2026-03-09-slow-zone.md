# SlowZone 減速地帶 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在地圖中隨機放置 2-3 個 SlowZone 格子（顯示 slow-speed.jpg），所有坦克進入後速度減半，離開後恢復。

**Architecture:** `SlowZone` 作為可穿越精靈由 `Map` 管理與繪製；`Game.update()` 每幀透過 `_apply_slow_zone_effects()` 偵測碰撞並動態修改坦克的 `speed`；`EnemyTank` 新增 `base_speed` 實例屬性作為速度恢復基準，`PlayerTank` 直接參照類別常數 `TANK_SPEED`。

**Tech Stack:** Python 3.11、pygame-ce、pathlib

---

### Task 1：新增 `SlowZone` 精靈類並整合進 `Map`

**Files:**
- Modify: `src/map.py`

**Step 1：在 `Map` 類靜態屬性區塊（約 212-215 行）新增 `slow_zone_image`**

在以下位置：
```python
    # 靜態圖像資源
    brick_image: Optional[pygame.Surface] = None
    steel_image: Optional[pygame.Surface] = None
    bush_image: Optional[pygame.Surface] = None
```
改為：
```python
    # 靜態圖像資源
    brick_image: Optional[pygame.Surface] = None
    steel_image: Optional[pygame.Surface] = None
    bush_image: Optional[pygame.Surface] = None
    slow_zone_image: Optional[pygame.Surface] = None
```

**Step 2：在 `Bush` 類下方（約第 172 行後）新增 `SlowZone` 精靈類**

```python
class SlowZone(pygame.sprite.Sprite):
    """
    減速地帶精靈類別

    坦克可穿越的地圖元素，進入後速度減半，離開後恢復。
    繪製在障礙物後、草叢前。

    屬性：
        rect: pygame.Rect - 碰撞區域
        image: pygame.Surface - 顯示圖像
    """

    SIZE = 40
    COLOR = (0, 0, 200)  # 回退顏色（藍色）

    def __init__(self, grid_x: int, grid_y: int) -> None:
        super().__init__()
        self.x = grid_x * self.SIZE
        self.y = grid_y * self.SIZE

        if Map.slow_zone_image is not None:
            self.image = pygame.transform.scale(Map.slow_zone_image, (self.SIZE, self.SIZE))
        else:
            self.image = pygame.Surface((self.SIZE, self.SIZE))
            self.image.fill(self.COLOR)

        self.rect = self.image.get_rect(topleft=(self.x, self.y))

    def draw(self, surface: pygame.Surface) -> None:
        surface.blit(self.image, self.rect)
```

**Step 3：在 `Map.__init__` 中載入圖像並初始化 slow_zones 群組**

在 `Map.__init__` 中找到載入圖像的部分（約 222-224 行）：
```python
        Map.brick_image = self._load_image("brick.png")
        Map.steel_image = self._load_image("wall.png")
        Map.bush_image = self._load_image("bush.png")
```
改為：
```python
        Map.brick_image = self._load_image("brick.png")
        Map.steel_image = self._load_image("wall.png")
        Map.bush_image = self._load_image("bush.png")
        Map.slow_zone_image = self._load_image("slow-speed.jpg")
```

在精靈組初始化區塊（約 227-230 行）新增：
```python
        self.slow_zones = pygame.sprite.Group()
```

在 `_generate_random_bushes()` 呼叫後新增：
```python
        self._generate_slow_zones()
```

**Step 4：新增 `_generate_slow_zones()` 方法**

在 `_generate_random_bushes` 方法後新增：
```python
    def _generate_slow_zones(self) -> None:
        """
        隨機生成地圖上的減速地帶

        - 隨機選擇 2-3 個格子放置 SlowZone
        - 避免在玩家起始區域和已有障礙物的位置放置
        - SlowZone 不阻擋坦克，但進入後減速
        """
        safe_zone_grid_x_min = self.PLAYER_SPAWN_X_MIN // self.GRID_SIZE
        safe_zone_grid_x_max = (self.PLAYER_SPAWN_X_MAX // self.GRID_SIZE) + 1
        safe_zone_grid_y_min = self.PLAYER_SPAWN_Y_MIN // self.GRID_SIZE
        safe_zone_grid_y_max = (self.PLAYER_SPAWN_Y_MAX // self.GRID_SIZE) + 1

        obstacle_positions = {
            (obstacle.x // self.GRID_SIZE, obstacle.y // self.GRID_SIZE)
            for obstacle in self.obstacles
        }

        available_positions = []
        for grid_y in range(self.MAP_HEIGHT):
            for grid_x in range(self.MAP_WIDTH):
                if not (
                    safe_zone_grid_x_min <= grid_x <= safe_zone_grid_x_max
                    and safe_zone_grid_y_min <= grid_y <= safe_zone_grid_y_max
                ):
                    if (grid_x, grid_y) not in obstacle_positions:
                        available_positions.append((grid_x, grid_y))

        num_slow_zones = random.randint(2, 3)
        if len(available_positions) >= num_slow_zones:
            selected = random.sample(available_positions, num_slow_zones)
        else:
            selected = available_positions

        for grid_x, grid_y in selected:
            slow_zone = SlowZone(grid_x, grid_y)
            self.slow_zones.add(slow_zone)
```

**Step 5：新增 `get_slow_zone_rects()` 方法**

在 `get_obstacles_rects` 方法後新增：
```python
    def get_slow_zone_rects(self) -> List[pygame.Rect]:
        """
        取得所有減速地帶的碰撞矩形

        返回：
            List[pygame.Rect] - 所有 SlowZone 的矩形列表
        """
        return [sz.rect for sz in self.slow_zones]
```

**Step 6：更新 `Map.draw()` 繪製 slow_zones**

找到 `draw` 方法（約 379-386 行）：
```python
    def draw(self, surface: pygame.Surface) -> None:
        self.obstacles.draw(surface)
```
改為：
```python
    def draw(self, surface: pygame.Surface) -> None:
        # 先繪製減速地帶（底層），再繪製障礙物
        self.slow_zones.draw(surface)
        self.obstacles.draw(surface)
```

**Step 7：啟動遊戲確認 SlowZone 圖案出現在地圖上**

```bash
cd /Users/roger/Desktop/Program-workspace/python3_workspace/Tank_war_pygame
source .venv/bin/activate
python main.py
```
預期：地圖上出現 2-3 個 slow-speed.jpg 圖案的格子，坦克可以走過去。

**Step 8：Commit**

```bash
git add src/map.py
git commit -m "feat(map): 新增 SlowZone 精靈類與地圖隨機生成"
```

---

### Task 2：`EnemyTank` 新增 `base_speed` 屬性

**Files:**
- Modify: `src/enemy.py`

**Step 1：在 `ENEMY_CONFIGS` 各 type 新增 `base_speed`**

找到（約 45-61 行）：
```python
    ENEMY_CONFIGS = {
        "basic": {
            "speed": 2,
            "lives": 1,
            "color": (255, 0, 0),
        },
        "fast": {
            "speed": 4,
            "lives": 1,
            "color": (0, 255, 0),
        },
        "heavy": {
            "speed": 2,
            "lives": 2,
            "color": (128, 128, 128),
        },
    }
```
改為：
```python
    ENEMY_CONFIGS = {
        "basic": {
            "speed": 2,
            "base_speed": 2,
            "lives": 1,
            "color": (255, 0, 0),
        },
        "fast": {
            "speed": 4,
            "base_speed": 4,
            "lives": 1,
            "color": (0, 255, 0),
        },
        "heavy": {
            "speed": 2,
            "base_speed": 2,
            "lives": 2,
            "color": (128, 128, 128),
        },
    }
```

**Step 2：在 `__init__` 中初始化 `base_speed`**

找到（約 93-94 行）：
```python
        self.speed = config["speed"]
        self.lives = config["lives"]
```
改為：
```python
        self.speed = config["speed"]
        self.base_speed = config["base_speed"]
        self.lives = config["lives"]
```

**Step 3：Commit**

```bash
git add src/enemy.py
git commit -m "feat(enemy): 新增 base_speed 屬性作為速度恢復基準"
```

---

### Task 3：`Game` 新增 `_apply_slow_zone_effects()` 並整合進 `update()`

**Files:**
- Modify: `src/game.py`

**Step 1：在 `_check_game_over` 方法前新增 `_apply_slow_zone_effects()`**

```python
    def _apply_slow_zone_effects(self) -> None:
        """
        偵測所有坦克與減速地帶的碰撞，動態調整移動速度。

        進入 SlowZone：速度 = base_speed * 0.5
        離開 SlowZone：速度 = base_speed
        """
        slow_zone_rects = self.map.get_slow_zone_rects()

        # 玩家坦克速度調整
        player_in_slow = any(
            self.player.rect.colliderect(sz_rect) for sz_rect in slow_zone_rects
        )
        self.player.speed = (
            PlayerTank.TANK_SPEED * 0.5 if player_in_slow else PlayerTank.TANK_SPEED
        )

        # 敵人坦克速度調整
        for enemy in self.enemies:
            enemy_in_slow = any(
                enemy.rect.colliderect(sz_rect) for sz_rect in slow_zone_rects
            )
            enemy.speed = enemy.base_speed * 0.5 if enemy_in_slow else enemy.base_speed
```

**Step 2：在 `update()` 的 `_check_collisions()` 呼叫前新增 `_apply_slow_zone_effects()`**

找到（約 214-218 行）：
```python
        # 檢查碰撞
        self._check_collisions()

        # 檢查遊戲結束條件
        self._check_game_over()
```
改為：
```python
        # 套用減速地帶效果
        self._apply_slow_zone_effects()

        # 檢查碰撞
        self._check_collisions()

        # 檢查遊戲結束條件
        self._check_game_over()
```

**Step 3：啟動遊戲驗證**

```bash
python main.py
```
驗證行為：
1. 地圖上有 slow-speed.jpg 格子
2. 玩家坦克開進去後明顯變慢（速度從 4 → 2）
3. 離開格子後恢復正常速度
4. 敵人坦克進入後也變慢，離開後恢復

**Step 4：Commit**

```bash
git add src/game.py
git commit -m "feat(game): 新增 SlowZone 減速效果，進入減速、離開恢復"
```
